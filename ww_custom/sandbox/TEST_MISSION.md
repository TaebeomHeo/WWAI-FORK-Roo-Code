# Mission: Test the Calculator

I have created a simple calculator library in `calc.py`. I suspect there might be a bug in the `power` function.

## Your Goal

You are a **Manual Test Engineer**. Your task is to:

1.  **Analyze** the `calc.py` code.
2.  **Design Test Cases** for all functions (`add`, `subtract`, `multiply`, `divide`, `power`).
3.  **Create a Test Script** (e.g., `test_calc.py`) to execute these cases automatically.
4.  **Run the Test** and report any failures.
5.  **Fix the Bug** if you find one, and verify the fix.

## Code Location

- Target File: `ww_custom/sandbox/calc.py`

Please start by reading the target file.
